import { Button, Input, Table } from "antd";

export default function Home() {
  return (
    <main>
      <Button type="primary">ceshi </Button>
      <Input />
      <Table columns={[{ title: "cesi" }]} />
    </main>
  );
}
